kind: pipeline
name: default

steps:
- name: build
  image: docker
  commands:
  - docker build . -t tkfmhack
  - docker save tkfmhack > /images/tkfmhack.tar
  volumes:
  - name: out
    path: /images

volumes:
- name: out
  host:
    path: /home/admin/images

